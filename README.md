# Cereveyn_PS3
This project demonstrates an autonomous agent architecture designed to solve task-oriented queries like "Book a meeting and notify the team." Unlike traditional scripts, this agent uses a ReAct (Reason + Act) pattern to handle environmental feedback, manage dependencies, and recover from API failures dynamically
